services:
  timescaledb:
    image: timescale/timescaledb-ha:pg16
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres
    ports:
      - "5555:5432"
    volumes:
      - ./initdb/create_image_search_service.sql:/docker-entrypoint-initdb.d/create_image_search_service.sql